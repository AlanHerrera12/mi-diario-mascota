import { View, Text } from 'react-native';

// TODO (Fase 2): Crear perfil del niño + elegir mascota inicial
// Campos: nombre/apodo, rango de edad, especie, nombre de mascota, colores
export default function ChildSetupScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white px-6">
      <Text className="text-2xl font-bold text-gray-800">Conoce a tu mascota</Text>
      <Text className="text-gray-500 mt-2">— Fase 2 pendiente —</Text>
    </View>
  );
}
