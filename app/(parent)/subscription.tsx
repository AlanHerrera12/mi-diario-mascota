import { View, Text } from 'react-native';

// TODO (Fase 6): Gestión de suscripción con RevenueCat
// - Estado actual (trial/active/expired)
// - Botón de upgrade / cancelar
export default function SubscriptionScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-50 px-6">
      <Text className="text-2xl font-bold text-gray-800">Mi suscripción</Text>
      <Text className="text-gray-500 mt-2">— Fase 6 pendiente —</Text>
    </View>
  );
}
