import { View, Text } from 'react-native';

// TODO (Fase 5): Pantalla de ingreso de PIN para acceder al dashboard parental
// Requiere: email + password ya autenticados + PIN de 4-6 dígitos
export default function ParentPinScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white px-6">
      <Text className="text-2xl font-bold text-gray-800">Ingresá tu PIN</Text>
      <Text className="text-gray-500 mt-2">— Fase 5 pendiente —</Text>
    </View>
  );
}
